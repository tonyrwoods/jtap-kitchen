{
  "name": "Order",
  "type": "object",
  "properties": {
    "table_number": {
      "type": "number",
      "title": "Table Number"
    },
    "server_name": {
      "type": "string",
      "title": "Server Name"
    },
    "items": {
      "type": "array",
      "title": "Order Items",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "quantity": {
            "type": "number"
          },
          "notes": {
            "type": "string"
          }
        }
      }
    },
    "status": {
      "type": "string",
      "title": "Status",
      "enum": [
        "New",
        "Preparing",
        "Ready",
        "Served"
      ],
      "default": "New"
    },
    "notes": {
      "type": "string",
      "title": "Special Notes"
    },
    "priority": {
      "type": "string",
      "title": "Priority",
      "enum": [
        "Normal",
        "Rush"
      ],
      "default": "Normal"
    }
  },
  "required": [
    "table_number",
    "status"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "staff"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "staff"
              }
            },
            {
              "data.server_name": "{{user.full_name}}"
            }
          ]
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "staff"
              }
            },
            {
              "data.server_name": "{{user.full_name}}"
            }
          ]
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}